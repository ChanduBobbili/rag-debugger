[project]
name = "rag-test-app"
version = "0.1.0"
description = "Test application for the RAG Debugger SDK"
requires-python = ">=3.10"
dependencies = [
    "rag-debugger",
    "httpx",
]

[tool.hatch.build.targets.wheel]
packages = ["."]
